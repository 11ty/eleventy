// Saves ~26KB (minified)
export default function () {
	throw new Error(
		"The `slugify` filter not included with the `@11ty/client` bundle. You can add it yourself via `eleventyConfig.addFilter` or use the `@11ty/client/eleventy` bundle.",
	);
}
